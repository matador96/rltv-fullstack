const getSumMatches = (player) => {
  return;
};

// const getGoalShotRatio = (player) => {

//     return avatars[platform];
//   };

//   const getFavoriteMode = (player) => {

//     return avatars[platform];
//   };

//   const getWorldPlace = (player) => {

//     return avatars[platform];
//   };

export { getSumMatches };
